From 3a288f01c379be4454ecaa0cb5d2d2494ba719e6 Mon Sep 17 00:00:00 2001
From: pershoot <28478728-pershoot@users.noreply.gitlab.com>
Date: Mon, 5 Jan 2026 22:01:51 -0500
Subject: [PATCH] kernel_patches: fs/susfs: Allow core to be built with no
 features

---
 kernel_patches/fs/susfs.c | 4 ++--
 1 file changed, 2 insertions(+), 2 deletions(-)

diff --git a/kernel_patches/fs/susfs.c b/kernel_patches/fs/susfs.c
index 043053f..7e54ebf 100644
--- a/kernel_patches/fs/susfs.c
+++ b/kernel_patches/fs/susfs.c
@@ -1142,7 +1142,7 @@ out_copy_to_user:
 }
 
 /* get susfs enabled features */
-static int copy_config_to_buf(const char *config_string, char *buf_ptr, size_t *copied_size, size_t bufsize) {
+static __maybe_unused int copy_config_to_buf(const char *config_string, char *buf_ptr, size_t *copied_size, size_t bufsize) {
 	size_t tmp_size = strlen(config_string);
 
 	*copied_size += tmp_size;
@@ -1157,7 +1157,7 @@ static int copy_config_to_buf(const char *config_string, char *buf_ptr, size_t *
 void susfs_get_enabled_features(void __user **user_info) {
 	struct st_susfs_enabled_features *info = (struct st_susfs_enabled_features *)kzalloc(sizeof(struct st_susfs_enabled_features), GFP_KERNEL);
 	char *buf_ptr = NULL;
-	size_t copied_size = 0;
+	__maybe_unused size_t copied_size = 0;
 
 	if (!info) {
 		info->err = -ENOMEM;
-- 
GitLab

